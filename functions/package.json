{
  "name": "aeo-dashboard-functions",
  "description": "Firebase Cloud Functions for AEO Dashboard",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "resend": "^4.0.0"
  }
}
